// Stub file - Discord channel removed

export function listDiscordDirectoryGroupsLive(): Promise<never[]> {
  return Promise.resolve([]);
}

export function listDiscordDirectoryPeersLive(): Promise<never[]> {
  return Promise.resolve([]);
}
