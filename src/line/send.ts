// Stub file - LINE channel removed

export function sendMessageLine(): Promise<{ ok: false; error: string }> {
  return Promise.resolve({ ok: false, error: "LINE channel removed" });
}

export function pushMessageLine(): Promise<{ ok: false; error: string }> {
  return Promise.resolve({ ok: false, error: "LINE channel removed" });
}

export function pushMessagesLine(): Promise<{ ok: false; error: string }> {
  return Promise.resolve({ ok: false, error: "LINE channel removed" });
}

export function pushFlexMessage(): Promise<{ ok: false; error: string }> {
  return Promise.resolve({ ok: false, error: "LINE channel removed" });
}

export function pushTemplateMessage(): Promise<{ ok: false; error: string }> {
  return Promise.resolve({ ok: false, error: "LINE channel removed" });
}

export function pushLocationMessage(): Promise<{ ok: false; error: string }> {
  return Promise.resolve({ ok: false, error: "LINE channel removed" });
}

export function pushTextMessageWithQuickReplies(): Promise<{ ok: false; error: string }> {
  return Promise.resolve({ ok: false, error: "LINE channel removed" });
}

export function createQuickReplyItems(): never[] {
  return [];
}
