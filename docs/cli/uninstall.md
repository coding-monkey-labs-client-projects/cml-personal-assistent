---
summary: "CLI reference for `cml-hive-assist uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `cml-hive-assist uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
cml-hive-assist uninstall
cml-hive-assist uninstall --all --yes
cml-hive-assist uninstall --dry-run
```
